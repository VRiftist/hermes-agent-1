# Hermes Key Vault Template — ~/.hermes/.env.template
# Copy to .env and fill in your keys:
#   cp .env.template .env
#   chmod 600 .env
#   # then edit .env with your real keys
#
# NEVER commit .env to git. .env is in .gitignore.

# === Cloud Providers ===
DEEPSEEK_API_KEY=
XAI_API_KEY=
OPENROUTER_KEY_1=
OPENROUTER_KEY_2=
KIMI_API_KEY=  # cold standby — leave empty until new key arrives

# === Optional / Future ===
FIRECRAWL_API_KEY=
BRAVE_API_KEY=
ANTHROPIC_API_KEY=
TWITTER_BEARER_TOKEN=